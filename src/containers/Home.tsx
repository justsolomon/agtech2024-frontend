import { Layout } from 'components/global';
import { UserTypeSelect } from 'components/home';

const HomeContainer = () => {
  return (
    <Layout>
      <UserTypeSelect />
    </Layout>
  );
};

export default HomeContainer;
